const initiateButton = document.querySelector('#initiate'); const urlInput = document.querySelector('#url-input'); const errorDisc = document.getElementById("error-disc");
initiateButton.addEventListener('click', () => { const url = urlInput.value.trim(); if (url !== "") { const gatewayUrl = `/search/query/?url=${encodeURIComponent(url)}`; location.href = gatewayUrl; } else { errorDisc.textContent = "URL cannot be empty."; errorDisc.style.display = "inherit"; } });
urlInput.addEventListener('keyup', (e) => { if (e.key === "Enter") { const url = urlInput.value.trim(); if (url !== "") { const gatewayUrl = `/search/query/?url=${encodeURIComponent(url)}`; location.href = gatewayUrl; } else { errorDisc.textContent = "URL cannot be empty."; errorDisc.style.display = "inherit"; } } });
